class INPUTS{
    constructor(){
        this.buffer = {jmp:0,use:0,l:0,u:0,d:0,r:0,rjmp:0,ruse:0,rl:0,ru:0,rd:0,rr:0};
        this.onKeyPress = new EventTarget();
    }
    update(){
        if (1);
            
        //window.addEventListener("keydown", function(e) { if(["Space","ArrowUp","ArrowDown","ArrowLeft","ArrowRight"].indexOf(e.code) > -1) {e.preventDefault();}}, false);
    }
}